import {
    hasNewsletter, shouldShowMenu, hasGuestbook, hasCredentials, hasPages,
} from "./author";

export {
    hasNewsletter as authorHasNewsletter,
    shouldShowMenu as shouldShowAuthorMenu,
    hasGuestbook as authorHasGuestbook,
    hasCredentials as authorHasCredentials,
    hasPages as authorHasPages,
};
