<%= react_component("AuthorTip", props: {
    displayAuthor: @display_author.as_json(
        only: [:display_name],
        methods: [:title, :url],
        include: {
            credentials: {
                only: [:id, :key, :value]
            }
        }
    )
}) %>
