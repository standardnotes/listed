<%= react_component("AuthorAll", props: {
    posts: @posts.as_json(
        only: [:id, :title, :unlisted, :page, :created_at, :word_count],
        methods: [:author_relative_url, :preview_text, :rendered_text]
    ),
}) %>
