<%= react_component("AuthorsMailerNewReaction", props: {
    reaction: @reaction,
    author: @author.as_json(
        only: :display_name
    ),
    post: @post.as_json(
        only: [:id, :title, :url]
    ),
}) %>
