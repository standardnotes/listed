<%= react_component("SharedFooter", props: {
    blogPage: @blog_page,
    author: (@display_author || @author || @post && @post.author if !@post || !@post.unlisted).as_json(
        methods: [:title],
        only: [:title],
    ),
    privatePost: @post && @post.unlisted
}) %>
