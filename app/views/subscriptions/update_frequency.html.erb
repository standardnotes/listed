<%= react_component("SubscriptionUpdateFrequency", props: {
    subscription: @subscription.as_json(
        only: [],
        include: { 
            author: { 
                methods: :title 
            }
        }
    )
}) %>
