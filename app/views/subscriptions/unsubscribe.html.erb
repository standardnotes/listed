<%= react_component("SubscriptionUnsubscribe", props: {
    subscription: @subscription.as_json(
        only: [],
        include: { 
            author: { 
                methods: :title 
            }
        }
    )
}) %>
