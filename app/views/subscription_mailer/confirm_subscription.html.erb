<%= react_component("SubscriptionMailerConfirmSubscription", props: {
    authorUrl: @author_url,
    confirmUrl: @confirm_url,
    author: @author.as_json(
        only: [],
        methods: :title
    )
}) %>
