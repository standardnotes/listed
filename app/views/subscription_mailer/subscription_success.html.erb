<%= react_component("SubscriptionMailerSubscriptionSuccess", props: {
    authorUrl: @author_url,
    unsubscribeUrl: @unsubscribe_url,
    author: @author.as_json(
        only: [],
        methods: :title
    )
}) %>
