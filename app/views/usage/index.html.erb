<%= react_component("Usage", props: {
    activeAuthors: @active_authors.as_json(
        only: [:id, :featured, :bio, :title, :last_word_count],
        methods: [:url, :title]
    )
}, prerender: false) %>
